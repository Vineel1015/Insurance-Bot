# never commit documents or model outputs
eval/letters/*
!eval/letters/.gitkeep
eval/results/
uploads/
*.env
.env*
__pycache__/
node_modules/

# reminder case state and outbox contain contact info (email/phone) — never commit
reminders/state/*
!reminders/state/.gitkeep
reminders/outbox.jsonl
